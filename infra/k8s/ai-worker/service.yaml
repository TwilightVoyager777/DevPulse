apiVersion: v1
kind: Service
metadata:
  name: ai-worker
  namespace: devpulse
  labels:
    app: ai-worker
spec:
  selector:
    app: ai-worker
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP
